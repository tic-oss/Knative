apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: react-app
spec:
  template:
    spec:
      containers:
        - image: raxkumar/react-app
          ports:
            - containerPort: 80